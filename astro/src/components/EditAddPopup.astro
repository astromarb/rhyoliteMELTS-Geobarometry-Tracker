<!-- EDIT-MODE ADD POPUP (body-level to escape nav overflow clipping) -->
<div id="edit-add-popup">
  <div id="eap-body">
    <button class="btn btn-primary btn-sm" data-action="openNewSimset">＋ Run</button>
    <button class="btn btn-sm eap-sample-btn" data-action="openAddSample">＋ Sample</button>
    <button class="btn btn-sm btn-primary" data-action="openModal" data-a1="modal-add-batch">＋ Batch</button>
  </div>
</div>

<style>
  .eap-sample-btn{background:var(--blue-dim);border-color:var(--blue);color:#fff}
</style>
